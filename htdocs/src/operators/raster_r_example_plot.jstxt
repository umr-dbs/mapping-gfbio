# TODO: check if mapping.pointscount >= 1
# Load the first point collection (yes, we're indexing starting at 0)
points = mapping.loadPoints(0, mapping.qrect);

# Now we can just draw the plot.
# The device is already initialized, don't mess with it!

# In this example, just plot the coordinates.
# Even though we could just look at the map instead...

plot(points@coords);

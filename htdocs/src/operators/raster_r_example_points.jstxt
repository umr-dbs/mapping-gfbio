# TODO: check if mapping.pointscount >= 1
# Load the first point collection (yes, we're indexing starting at 0)
points = mapping.loadPoints(0, mapping.qrect);
# TODO: do something with them

# return the points
points;